import React from "react";
import "./style.css";

const AdminSeedButton = (props) => {
  return (
    <button id="admin-seed" className="pure-button pure-button-primary">Seed Database</button>
  );
};

export default AdminSeedButton;